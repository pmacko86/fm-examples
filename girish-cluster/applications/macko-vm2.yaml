apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: macko-vm2
  namespace: allclusters
spec:
  targets:
    - clusterGroup: rtp-dev
      name: rtp-dev
  branch: main
  repo: https://github.com/pmacko86/fm-examples
  paths:
    - girish-cluster/applications/macko-vm2
